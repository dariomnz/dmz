// RUN: (dmz %s -res-dump 2>&1 || true) | filecheck %s
const S = struct {
};
const S2 = struct {
};

fn main() -> void {
    let x = S{};

    // CHECK: [[# @LINE + 1 ]]:9: error: unexpected type in condition 'struct_in_condition.S{}'
    if (x) {
    }

    // CHECK: [[# @LINE + 1 ]]:12: error: unexpected type in condition 'struct_in_condition.S{}'
    while (x) {
    }
}