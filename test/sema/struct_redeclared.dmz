// RUN: (dmz %s -res-dump 2>&1 || true) | filecheck %s
const S = struct {
    x: i32,
};

fn S() -> void {
}

const S = struct {
    // CHECK: [[# @LINE - 1 ]]:7: error: redeclaration of 'S'

    x: i32,
};

fn main() -> void {
}