// RUN: (dmz %s -res-dump 2>&1 || true) | filecheck %s
const S = struct {
    x: i32,
};

fn main() -> void {
    let s = S{x: 1};

    !s;
    // CHECK: [[# @LINE - 1 ]]:6: error: 'struct_in_unary.S{}' cannot be used as an operand to unary operator '!'
}