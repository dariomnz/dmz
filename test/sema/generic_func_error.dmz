// RUN: (dmz %s -res-dump 2>&1 || true) | filecheck %s

fn foo(param: i32) -> anytype {
    return param;
}

fn main() -> void {
    foo(2);
    // CHECK: error: could not infer generic type for 'anytype'
}