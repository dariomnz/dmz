// RUN: (dmz %s -res-dump 2>&1 || true) | filecheck %s
const S = struct {
    x: i32,
};

const S2 = struct {
    x: i32,
};

fn foo(s: S) -> void {
}

fn main() -> void {
    foo(S{x: 0});
    foo(S2{x: 0});
    // CHECK: [[# @LINE - 1 ]]:9: error: unexpected type of argument
}