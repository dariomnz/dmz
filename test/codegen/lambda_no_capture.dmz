// RUN: dmz %s -run 2>&1 | filecheck %s
extern fn printf(fmt: *u8, ...) -> i32;

fn main() -> void {

    const lambda = struct {
        pub fn call(a: i32) -> i32 { return 10 + a; }
    };
    const f = lambda.call;

    // CHECK: 15
    printf("%d\n", f(5));
}